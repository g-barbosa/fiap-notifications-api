# Service para a Notifications API
apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: fiap-cloud-games
  labels:
    app.kubernetes.io/name: notifications-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: fiap-cloud-games
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: notifications-api
